package main

// This package comes from internal/cmd/[api.go, worker.go, scheduler.go]
// Each of those files exposes a cobra command that can be executed.
// "github.com/hbbb/go-backend-template/cmd"

func main() {
	// ctx, cancel := signal.NotifyContext(context.Background(), os.Interrupt, os.Kill)
	// defer cancel()

	// ret := cmd.Execute(ctx)
	// os.Exit(ret)
}
